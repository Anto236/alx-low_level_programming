#include <stdio.h>
/**
 * main - prints all single numbers from 0 to 10 without char
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	{
	putchar("\n");
	return (0);
	}
}
